const Form = (props) => {

    return (

        <input
            className="form-control"
            type="text"
            placeholder={props.textform}
            aria-label="default input example"
        />

    );

}

export default Form;